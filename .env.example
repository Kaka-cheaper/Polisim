# Polisim 环境变量模板（session 45）
#
# 用法：
#   1. 把本文件复制为 `.env`：
#        Copy-Item .env.example .env
#   2. 填上你真实的 API key
#   3. 跑 `python -m cli serve --port 8000` 或 `python -m cli run ...`
#      —— 程序自启会自动加载 .env，无需手动 export
#
# 安全：
#   - `.env` 已在 .gitignore 中，不会被提交到 git
#   - 永远不要把真实 key 写进 `config/llm.yaml`（那个字段是变量名，不是 key）
#   - 永远不要把真实 key 提交到任何 .md / 代码文件 / chat 历史
#
# 字段说明：
#   每个变量名必须与 config/llm.yaml 中某个 provider 条目的 `api_key_env` 字段对齐。
#   不需要的条目可以删掉或留空——但留空时对应 provider 调用会报错。

# 走 vveai.com 代理（你当前 default_provider 指向的条目）
VVEAI_API_KEY=sk-your-real-key-here

# 走官方 OpenAI（备用条目，仅在 --provider-key openai_gpt4mini 时使用）
# OPENAI_API_KEY=sk-your-openai-key

# 走 OpenRouter（备用）
# OPENROUTER_API_KEY=sk-or-your-openrouter-key
